import ButtonList from "./ButtonList";
import VideoContainer from "./VideoContainer";

const MainContainer = () => {
  return (
    <main className="px-8 my-2 ">
      <ButtonList />
      <VideoContainer />
    </main>
  );
};

export default MainContainer;
